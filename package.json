{
  "name": "final-project-root",
  "version": "1.0.0",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "npx concurrently \"npm run server\" \"npm run db\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}